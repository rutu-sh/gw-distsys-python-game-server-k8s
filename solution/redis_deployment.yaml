apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7
          command:
          - redis-server
          resources:
            requests:
              cpu: "125m"
              memory: 256Mi
            limits:
              cpu: "125m"
              memory: 256Mi
          ports:
            - containerPort: 6379